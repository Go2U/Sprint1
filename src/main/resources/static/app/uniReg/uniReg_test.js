'use strict';

describe('myApp.uniReg module', function() {

  beforeEach(module('myApp.uniReg'));

  describe('uniReg controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var uniRegCtrl = $controller('uniRegCtrl');
      expect(uniRegCtrl).toBeDefined();
    }));

  });
});