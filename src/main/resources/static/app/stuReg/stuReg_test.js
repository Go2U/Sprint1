'use strict';

describe('myApp.stuReg module', function() {

  beforeEach(module('myApp.stuReg'));

  describe('stuReg controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stuRegCtrl = $controller('stuRegCtrl');
      expect(stuRegCtrl).toBeDefined();
    }));

  });
});